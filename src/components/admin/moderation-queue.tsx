// Pending deals list with approve/reject buttons
// TODO: Junior — fetch pending deals, render each with title, author, category,
//   Approve (green) and Reject (red) buttons calling moderateDeal server action
export function ModerationQueue() {
  return <div>TODO: ModerationQueue</div>
}
