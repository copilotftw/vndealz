// Dashboard stat cards grid
// TODO: Junior — fetch stats via getDashboardStats(), render 4-6 cards
//   Each card: icon, label, number. Use glass background.
export function StatsCards() {
  return <div className="grid grid-cols-2 lg:grid-cols-4 gap-[var(--card-gap)]">TODO: stats</div>
}
