// Page navigation
// TODO: Junior — accept currentPage, totalPages, render prev/next + page numbers as Links
export function Pagination({ currentPage, totalPages }: { currentPage: number; totalPages: number }) {
  return <div>TODO: Pagination</div>
}
